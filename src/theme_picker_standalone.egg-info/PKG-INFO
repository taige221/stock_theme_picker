Metadata-Version: 2.4
Name: theme-picker-standalone
Version: 0.1.0
Summary: Standalone theme-based stock picker extracted from daily_stock_analysis
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.115
Requires-Dist: uvicorn>=0.30
Requires-Dist: pydantic>=2.7
Requires-Dist: pandas>=2.2
Requires-Dist: numpy>=1.26
Requires-Dist: requests>=2.32
Requires-Dist: SQLAlchemy>=2.0
Requires-Dist: tenacity>=9.0
Requires-Dist: python-dotenv>=1.0
Requires-Dist: newspaper3k>=0.2.8
Requires-Dist: fake-useragent>=1.5
Requires-Dist: akshare>=1.16
Requires-Dist: efinance>=0.5.5
Requires-Dist: yfinance>=0.2.50
Requires-Dist: baostock>=0.8.9
Requires-Dist: pytdx>=1.72
Provides-Extra: dev
Requires-Dist: pytest>=8.3; extra == "dev"

# Theme Picker

`theme_picker/` 现在已经按“可独立仓库运行”的目标收口，不再只是主仓库里的一个迁移骨架。

当前 Python 包体已经采用标准 `src` 布局：

- `src/theme_picker/`：真正的 Python 包代码
- 根目录仅保留启动入口、打包文件、README、兼容桥与 `web/`

当前目录具备这些能力：

- 独立 FastAPI 入口：
  - `server.py`
  - `main.py --serve`
- 独立配置：
  - `config.py`
  - `.env.example`
- 独立 SQLite 存储：
  - `storage.py`
  - 默认数据库 `./data/stock_analysis.db`
- 独立主题数据与缓存：
  - `data/theme_registry.json`
  - `data/theme_board_mappings.json`
  - `data/theme_board_cache.json`
- 独立搜索 / 技术分析 / 行情提供层：
  - `search_service.py`
  - `stock_analyzer.py`
  - `data_provider/`
  - `patch/`
- 独立前端子仓库骨架：
  - `web/`
  - 内含 `theme-picker` 页面、API 封装、Vite/Tailwind/Vitest 配置

同时保留了和主仓库的兼容关系：

- 主仓库里的旧入口仍然可以继续 import `theme_picker.*`
- 但 `theme_picker/` 内部已经不再依赖 `src.storage` / `src.search_service` / `src.stock_analyzer` / `src.data.*`
- 配置和持久化也已经切到本地实现

## 目录说明

- `application/`：主题选股聚合、任务生命周期、主题注册
- `infrastructure/`：板块解析、事件扫描、扩池、技术筛选、股票池、运行时适配
- `core/`：主题选股管道编排
- `domain/`：领域模型
- `api/`：API schema 与 endpoint
- `data_provider/`：主题选股独立运行所需的数据源策略层
- `data/`：主题、板块映射、缓存和静态数据
- `web/`：主题选股独立前端子仓库骨架

以上这些 Python 目录现在都位于：

- `src/theme_picker/`

## 独立运行

1. 复制 `.env.example` 为 `.env`
2. 安装依赖

```bash
pip install -e .
```

或：

```bash
pip install -r requirements.txt
```

3. 启动服务

```bash
python main.py --serve
```

或者：

```bash
uvicorn server:app --host 127.0.0.1 --port 8765
```

服务默认暴露：

- `GET /health`
- `GET /api/v1/theme-picker/themes`
- `POST /api/v1/theme-picker/scan`
- `GET /api/v1/theme-picker/status/{task_id}`
- `GET /api/v1/theme-picker/history`
- `POST /api/v1/theme-picker/retry/{task_id}`

## 当前边界

这次已经做到“目录级独立运行”，但还没有做到真正的单独发布仓库标准化：

- 还没有独立 CI
- 还没有独立 Dockerfile
- 仍然保留主仓库兼容包装层

如果下一步继续推进，最自然的是：

1. 再补一个独立 `requirements.txt`
2. 再补 `tests/` 的最小 smoke
3. 最后再把主仓库里的兼容包装逐步降成只读桥接层
